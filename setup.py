from setuptools import setup
setup(name='network',
      version='0.1',
      description='This is kamal scanner module',
      long_description='This is kamal network scanner module',
      author='Prayag',
      packages=['network'],
      install_requires=['nmap', 'scapy']
      )
